# 경로 추천 실패 케이스 통합 논의 목록

| 항목 | 내용 |
| --- | --- |
| 작성 일자 | 2026-08-26 |
| 근거 조사 | Claude Code 전수 조사 (ploggo-server, Repo_JDH 8개 파일) |
| 총 항목 수 | 56건 (F-01~F-52 조사분 52건 + N-01~N-04 추가 지적 4건) |
| 목적 | 그룹별 순차 논의를 통한 조치 방향 결정 |
| 상태 | 논의 전 |

---

## 0. 조사 개요

### 대상 파일

| 약칭 | 경로 | 행 수 |
| --- | --- | --- |
| ALG | `ploggo-server/route_recommender/algorithm.py` | 503 |
| RTR | `ploggo-server/route_recommender/router.py` | 92 |
| CFG | `ploggo-server/route_recommender/config.py` | 48 |
| SRV | `ploggo-server/server.py` | 532 |
| PREP | `ploggo-server/scripts/prepare_hotspots.py` | 85 |
| REPO | `Repo_JDH/lib/features/plogging/data/route_repository.dart` | 106 |
| MODEL | `Repo_JDH/lib/features/plogging/domain/route_models.dart` | 82 |
| SCR | `Repo_JDH/lib/features/plogging/presentation/route_setup_screen.dart` | 약 640 |

### 방어 수준 분류

| 분류 | 건수 | 해당 항목 |
| --- | --- | --- |
| 방어됨 | 6 | F-22, F-34, F-37, F-38, F-44, F-51 |
| 부분 방어 | 11 | F-01, F-02, F-03, F-06, F-16, F-24, F-25, F-26, F-27, F-46, F-49 |
| 미방어 | 35 | 나머지 전부 |

### 사전 확인 완료 사항

F-29(빈 polyline이 성공으로 반환)가 v3~v5 실험 데이터에 혼입되었는지 확인한 결과, **혼입되지 않았다.** `step6_k2_200od.csv`의 도보 거리 최솟값은 1.684km, `step6_gamma_200od_realistic.csv`는 2.233km이며, 완전 제거로 집계된 39건·37건 모두 도보 거리가 2km 이상이다. 따라서 기존 실험 수치는 이 문제와 무관하게 유효하다.

---

## A그룹 — 검증 결과의 유효성에 영향

이 그룹을 먼저 결정해야 나머지 논의가 의미를 갖는다. 지금까지의 실험 수치가 운영 환경에서 재현되는지가 걸려 있다.

| # | 내용 | 위치 | 논의 포인트 |
| --- | --- | --- | --- |
| F-12 | 앱이 `district`를 보내지 않아 군구 판정 단계가 운영에서 미실행. 항상 전체 1,535곳 + buffer 1.5km 적용 | SCR:419, SCR:435-440 | 검증 조건과 운영 조건이 다르다. 앱이 보내게 할지, 전체 대상이 설계 의도인지 결정 필요 |
| F-13 | 미지 `district`가 조용히 전체 핫스팟으로 대체 (`subset if len(subset)>0 else hotspots`) | ALG:460-461 | F-12와 같은 코드 라인. 폴백 유지 여부 |
| F-14 | `district`가 `Optional[str]` 자유 문자열, 검증 없음 | RTR:35-39 | 허용값 목록 제한 여부 |
| F-15 | buffer 반경이 강화군 2.5km / 나머지 1.5km 이분법 | ALG:150-152 | F-12로 인해 강화군 규칙이 사문화 상태 |
| N-01 | A그룹 결정에 따른 재검증 필요 범위 판단 | — | 조건을 맞추면 v3~v5 수치 유효. 다르게 가면 재측정 필요 |

---

## B그룹 — 알고리즘 설계 결함 (수정 시 재검증 필요)

"알고리즘 개선 여지 없음"이라는 기존 결론을 재검토하게 만드는 항목들이다.

| # | 내용 | 위치 | 논의 포인트 |
| --- | --- | --- | --- |
| F-40 | swap 대상 선택이 무의미해지는 구조. 페널티가 전부 0이면 `idxmax()`가 첫 행(Score 최고 후보)을 제거 | ALG:398 | 직선 기준 페널티가 0인데 실제 도로가 교차하는 경우 5회 시도가 방향성 없이 낭비. polyline 기준으로 변경 시 hard case 개선 가능성 |
| F-18 | `avoid_penalty=999`가 차단이 아님. `nlargest(k_eff)`이므로 후보 부족 시 회피구역 내부 핫스팟이 선정됨 | ALG:181-182, ALG:338 | 주석("완전 차단")과 실제 동작 불일치 |
| F-19 | `in_avoid_zone` 컬럼을 PREP가 계산해 CSV에 저장하나 ALG가 읽지 않음 | ALG:86, PREP:68-70 | F-18의 직접 원인이자 해결 수단. 수정 비용 낮음 |
| F-17 | `k_eff = min(k, n)` 조용한 축소. 응답에 축소 사실 없음 | ALG:337 | K 옵션 도입 시 필수. K=5에서 17쌍(8.9%) 미달 실측 |
| F-20 | S_i 상위 15개 밖 후보 배제 | ALG:331 | 설계 의도이나 K 상향 시 대체 후보 부족으로 연결 |
| N-02 | 경로 길이 상한 부재 | — | K=2도 평균 5.03km(3km/h 기준 약 96분). 30분 목표와 격차 |

---

## C그룹 — 조용한 실패 (오류 없이 잘못된 결과)

| # | 내용 | 위치 | 논의 포인트 |
| --- | --- | --- | --- |
| F-29 | features는 있으나 LineString 없으면 빈 경로가 `status:"OK"` | ALG:268-281 | 실험 데이터 미혼입 확인됨. 운영 방어는 별도 필요 |
| F-33 | path 2점 미만이면 "교차 없음"으로 반환 | ALG:286-289 | F-29와 연쇄 |
| F-50 | 앱 ready 판정이 `result != null`뿐. polyline 비어도 시작 버튼 활성 | SCR:458, SCR:626-630 | F-29→F-33→F-50 연쇄. 서버·앱 양쪽 무방어 |
| F-39 | swap 중 Tmap 실패 시 `error`가 응답에서 소실. 스키마에 필드 없음 | ALG:365-372, ALG:479, RTR:50-61 | hard case 68%에 Tmap 실패가 섞였는지 분리 불가 |
| F-42 | `ALGO_MAX_SWAP` 음수면 `return None` → 호출부가 "후보 없음"으로 오인 | ALG:360, ALG:412, ALG:475-476 | 설정 오류가 조용한 실패로 |
| F-30 | `totalDistance` 누락 시 `.get(...,0)`으로 0 대입 | ALG:265-266 | 거리 0m가 정상값으로 반환 |
| F-36 | UQA310/320/330만 by_code 등록. 그 외 코드는 교차해도 목록 빈 배열 | ALG:69 | 147개 폴리곤의 코드 분포 확인 필요 |
| F-23 | S_i에 NaN 있으면 `nlargest`가 조용히 제외 | ALG:331, ALG:338 | CSV 값 분포 실측 필요 |
| F-41 | swap 후 `to_frame().T`로 object dtype 변질 | ALG:409 | pandas 버전에 따라 TypeError 또는 잘못된 정렬 |
| F-16 | 후보 0건 → `_empty_route_response`. 서버는 정상 반환 | ALG:468-469, ALG:428-442 | 앱이 성공으로 표시 |

---

## D그룹 — 운영 관측 불가 (로깅)

| # | 내용 | 위치 | 논의 포인트 |
| --- | --- | --- | --- |
| F-24 | 네트워크 예외 → 문자열 치환, 로그 없음 | ALG:247-248 | 원인 소실 |
| F-25 | HTTP != 200. 401/429/400 구분 분기 없음. 502 detail에 Tmap 본문 150자 노출 | ALG:250-252, RTR:79 | 한도 초과 판별 불가 + 외부 응답 노출 |
| F-26 | JSON 파싱 실패 → "JSON_ERROR", 원문 미보존 | ALG:255-256 | simplejson 환경에서 미포착 가능성 |
| F-27 | 응답 type이 FeatureCollection 아님 → "INVALID" | ALG:258-259 | 기록 없음 |
| F-28 | features 빈 배열 → "NO_FEATURES" | ALG:261-262 | 200쌍 중 8쌍 실패가 여기로 수렴 추정 |
| F-46 | 앱이 `routeState.error`를 읽지 않고 고정 문구 출력 | SCR:543-548 | 모든 실패가 "도착지를 다시 맞춰주세요"로 붕괴. 한 줄 수정으로 상당 부분 회수 |
| F-43 | `swap_history` 계산하나 응답 스키마에 없음 | ALG:403-407 | 진단 정보 유실 |
| F-52 | `bestFromAttempt`·`nSwaps` 등 파싱만 하고 미사용 | MODEL:66-75 | 동일 |
| 총괄 | ALG·RTR·CFG 3개 파일 로그 0건 | — | 로깅 수준 정책 필요 |

---

## E그룹 — 입력 검증 부재

| # | 내용 | 위치 | 논의 포인트 |
| --- | --- | --- | --- |
| F-07 | 위경도 범위 초과. haversine이 18,074km 반환 | RTR:28-29, ALG:126-134 | pydantic 범위 제약 추가 여부 |
| F-08 | lat/lon 뒤바뀜. 인천 좌표 뒤집으면 중국 근해 | 동일 | 오류 없이 `success:true` |
| F-09 | NaN/Infinity 통과. Tmap payload에 NaN 직렬화 | RTR:28-29 | pydantic 2.13.2가 통과시킴(실측) |
| F-10 | 출발지 == 도착지 | ALG:160 | Tmap 거부 시 502 |
| F-11 | 인천 외 좌표. 후보 0건 → 경유지 없는 경로가 성공 반환 | ALG:467-469 | 앱은 "거점 0곳" + "위험 구간 없음" 표시하고 시작 허용 |

---

## F그룹 — 배포·기동 시 발현

| # | 내용 | 위치 | 논의 포인트 |
| --- | --- | --- | --- |
| F-04 | `warmup()` 호출부 0건(실측). 첫 요청이 1.76MB 파싱 부담 | ALG:107, SRV:21-22 | 기동 시 호출 추가 |
| F-05 | `TMAP_APP_KEY` 기본값 "". 기동 검증 없음 | CFG:35 | 전 요청 502 |
| F-01 | 회피구역 GeoJSON 부재 → FileNotFoundError, 라우터 미포착 | ALG:52-55 | 예외 메시지는 구체적이나 500으로 삼켜짐 |
| F-02 | 핫스팟 CSV 부재 | ALG:81-84 | 동일 |
| F-03 | CSV 필수 컬럼 누락 → ValueError | ALG:88-89 | 동일 |
| F-06 | `/route/health`가 데이터 로드를 트리거 → 결손 시 헬스체크도 500 | RTR:89-90 | 헬스체크로 결손 구분 불가 |
| F-47 | `!uri.hasPort`면 `:8000` 강제 부착. Dart는 기본 포트에서 false | REPO:42-45 | 도메인·HTTPS 전환 시 전량 실패. 현재 미발현 |
| F-21 | `ALGO_ALPHA` 등에 비숫자 → import 시점 예외 | CFG:13-18 | 기동 실패 |
| F-31 | 15초 × 6회 = 90초 단일 요청. 동기 함수라 스레드풀 점유 | CFG:37, ALG:360, RTR:68 | 앱 timeout 120초로 커버되나 서버 부하 |
| F-32 | 일시적 5xx 재시도 없음 | ALG:245-252 | 재시도 정책 |

---

## G그룹 — 정상 동작 또는 기존 인지 사항

| # | 내용 | 위치 | 비고 |
| --- | --- | --- | --- |
| F-22 | 경유지 6개 이상 선차단 | ALG:240-242 | 실측(상한 5개)이 코드에 반영됨 |
| F-34 | 길이 0 시 ratio 0 (ZeroDivision 회피) | ALG:298 | 정상 방어 |
| F-37 | 대체 후보 소진 → best 반환, hard_case=True | ALG:392-395 | 정상 |
| F-38 | swap 한도 도달 | ALG:392-395 | hard case 68%가 여기 |
| F-44 | success=false + polyline 없음 → 502 | RTR:76-80 | 정상 방어 |
| F-51 | hard case 경고 표시하되 시작 허용 | SCR:556-560 | 68%라 경고 상시화 |
| F-35 | 111,000 근사. 동서 약 26% 과대 추정 | ALG:296-297 | 기존 인지 사항 |
| N-03 | F-35 값이 "위험 구간 약 N m"로 사용자에게 직접 노출 | SCR:559 | `ALGO_HARD_CASE_M=691`도 같은 근사 위에서 산출 |
| F-45 | 502 detail에 Tmap 원문 150자 포함 | RTR:79, ALG:252 | 외부 API 응답 노출 |
| F-48 | polyline 원소 2개 미만 시 RangeError | MODEL:69-71, SCR:350 | 서버 변경 시 발현 |
| F-49 | `hotspot_id` null 시 캐스트 실패 | MODEL:23 | AsyncValue.guard 포착 → F-46 문구 |
| N-04 | 옹진군 실측 확인 — CSV `source_district`는 9종, `ongjin` 없음 | — | 옹진군 사용자는 육지 핫스팟 섞인 경로 수신 |

---

## 논의 순서

| 회차 | 그룹 | 핵심 안건 | 결정 사항 |
| --- | --- | --- | --- |
| 1 | A | F-12 (군구 판정 미실행) | 검증 조건 정합 방향, 재검증 범위 |
| 2 | B | F-40 (swap 무의미화), N-02 (거리 상한) | 알고리즘 개선 여부, 재검증 필요성 |
| 3 | C | F-29→F-33→F-50 연쇄, F-39 | 조용한 실패 차단 방식 |
| 4 | D·E | F-46, 로깅 정책, 입력 검증 수준 | 관측성 확보 범위 |
| 5 | F | 배포 전 체크리스트, F-47 | 기동 검증 및 전환 대비 |

각 회차는 결정 사항을 확정한 뒤 다음으로 넘어간다. B그룹 결정에 따라 재검증(v6)이 필요할 수 있다.
